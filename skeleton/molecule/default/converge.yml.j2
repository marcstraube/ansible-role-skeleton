---
- name: Converge
  hosts: all
  gather_facts: true

  vars:
    {{ role_name }}_enabled: true

  tasks:
    - name: Converge | Include {{ role_name }} role  # noqa: role-name[path]
      ansible.builtin.include_role:
        name: '{{ '{{' }} playbook_dir {{ '}}' }}/../..'
